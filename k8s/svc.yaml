# apiVersion: v1
# kind: Service
# metadata:
#   name: svc-dev
#   namespace: dev-ns
# spec:
#   type: LoadBalancer
#   selector:
#     app: dept-dev
#   ports:
#     - protocol: TCP
#       port: 80
#       targetPort: 80

      
apiVersion: v1
kind: Service
metadata:
  name: static-svc
  namespace: prod-ns
spec:
  type: LoadBalancer
  selector:
    app: static-dept
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80